version: '3.4'

services:
  bsky:
    image: bsky-feed
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - 5000:80
    volumes:
      - ./appsettings.json:/App/appsettings.json
      - ./bsky.db:/App/bsky.db
      - ./Keywords:/App/Keywords